# 人员效率分析口径说明

本文档说明创意周会仪表盘「人员效率」页面中各项指标的统计逻辑和使用场景。

## 0. 报表作用

用于评估广告投流业务中创意人员的效率表现。由于各项目不同素材类型的制作难度差异较大，本报表不直接使用平均耗时排名，而是通过 `基准耗时` 对任务难度进行校正。

## 1. 数据范围

当前仅分析广告投流业务下的图片和混剪素材，岗位范围包括设计和剪辑，实习生不含在内。

参与计算的明细需满足：

- 业务来源为广告投流
- 业务单元为图片或混剪
- 岗位为设计或剪辑
- 产出数大于 0
- 周报工作量大于 0
- 制作人员和统计年月不为空

## 2. 统计范围

结果按两类时间范围生成：

- 月度：每个统计年月单独建基准、单独计算人员指标
- 总体：使用全部筛选后数据重新建基准，再汇总人员实际耗时和基准耗时

总体不是月度效率的简单平均。

## 3. 业务单元口径

人员效率结果保留三类业务单元视角：

- 图片：只统计该人员图片业务单元数据
- 混剪：只统计该人员混剪业务单元数据
- 总计：合并该人员图片和混剪数据

仪表盘默认建议使用：

```text
统计范围 = 总体
业务单元 = 总计
```

用于先判断人员整体表现，再切换到图片或混剪定位具体业务单元。

## 4. 基础数据

### 实际耗时

制作部门提供的员工实际制作耗时，统计到项目和素材细分类型。

### 基准耗时

根据相似任务样本计算出的理论参考耗时，用于消除项目、素材细分类型以及行业差异带来的难度影响。

基准单素材耗时：

```text
基准单素材耗时 = 基准池总周报工作量 / 基准池总产出数
```

单条记录的基准耗时：

```text
单条基准耗时 = 该记录产出数 × 命中基准的基准单素材耗时
```

人员基准耗时：

```text
基准耗时 = Σ 单条基准耗时
```

## 5. 基准耗时计算逻辑

项目维度统计项目平均水平。

### 一级基准

优先使用同运营部门、同产品名称、同素材细分类型下的平均耗时作为参考，且参与人员不少于 3 人。

后台计算时会同时保留业务单元差异，避免图片和混剪被合并比较。

### 二级基准

若一级基准样本不足，则放宽到同行业、同素材细分类型下的平均耗时，且参与人员不少于 3 人。

后台计算时同样保留业务单元差异。

### 样本不足处理

若二级基准仍不满足要求，则该任务不参与效率评判。

## 6. 评估指标

### 评估效率

```text
评估效率 = 基准耗时 / 实际耗时
```

含义：

- `> 1`：实际耗时低于基准，效率高于平均参考水平
- `= 1`：接近基准水平
- `< 1`：实际耗时高于基准，效率低于平均参考水平

效率分类规则：

- 高效率：评估效率 >= 1.1
- 正常：0.9 < 评估效率 < 1.1
- 待提升：评估效率 <= 0.9
- 效率分类用于快速定位人员表现区间，具体原因需结合人员明细中的分项目情况具体分析。

### 节省工时

```text
节省工时 = 基准耗时 - 实际耗时
```

含义：

- `> 0`：相对基准节省耗时
- `= 0`：与基准一致
- `< 0`：超出基准耗时

### 基准覆盖率

```text
基准覆盖率 = 成功匹配到一级或二级基准的产出数 / 总产出数
```

基准覆盖率越高，评估结果越可靠。覆盖率较低时，应结合样本不足占比谨慎解读。

## 7. 页面使用建议

建议阅读顺序：

1. 先看顶部指标卡，判断整体高效率、正常、待提升人数分布。
2. 再看部门分类分布，定位高效率和待提升人员集中在哪些制作部门。
3. 查看人员效率主表，按统计范围、业务单元和制作部门筛选具体人员。
4. 点击人员姓名，查看该人员在不同项目和素材细分类型下的实际耗时、基准耗时和节省工时。
5. 最后查看需求量 TOP10 项目人员效率对比，判断大项目内不同人员和部门的效率差异。

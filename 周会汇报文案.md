# 创意周会周维度汇报文案


素材方面主要跟大家汇报下上周端午前后的产出情况


## 一、图片

先看图片总体。

总产出为 `11588`，周环比减少 `9.3%`；人均日均产出为 `95`，周环比增加 `13.4%`；饱和度为 `127%`，单素材耗时为 `6.0` 分钟。

因为上周端午放假一天，仅4个工作日，所以总产出减少，饱和度及人均日产出增加。

### 1. 内容二部

内容二部图片总产出为 `4736`，周环比减少 `14.0%`；人均日均产出为 `116`，周环比增加 `7.5%`；饱和度为 `118%`，单素材耗时为 `5.5` 分钟。

产出方面，主要受金融行业的 `K7 · 360借条-小程序` 影响，减少 `806` 张


### 2. 效果设计部

效果设计部图片总产出为 `4793`，周环比减少 `3.5%`；人均日均产出为 `96`，周环比增加 `20.6%`；饱和度为 `143%`，单素材耗时为 `6.0` 分钟。


### 3. 品牌设计部

品牌设计部图片总产出为 `1564`，周环比减少 `8.8%`；人均日均产出为 `65`，周环比增加 `14.1%`；饱和度为 `113%`，单素材耗时为 `7.4` 分钟，周环比增加 `31.0%`。

单素材耗时明显增加，主要因为高耗时项目产出增加，海报类型的项目：`销售培训部 · 图片制作` 平均 `240.0` 分钟/张，产出增加 `3` 张；
同时 `成都 · 城际停车物联信息(深圳)有限公司成都分公司的项目` 为新项目，单素材耗时 `120.0` 分钟/张，产出 `5` 张

### 4. 合肥创意部

合肥创意部图片总产出为 `495`，周环比减少 `15.4%`；人均日均产出为 `71`，周环比增加 `5.8%`；饱和度为 `109%`，

---

单素材人力成本目前更新到完整的5月数据，上次周会已经汇报过了，待下次周会更新完6月完整数据再跟大家汇报


## 二、混剪

混剪素材上周需求明显减少

上周总产出为 `3112`，周环比减少 `31.7%`，；人均日均产出为 `33`；饱和度为 `98%`；单素材耗时为 `13.2` 分钟。


### 1. 内容二部

内容二部混剪总产出为 `1784`，周环比减少 `36.2%`；人均日均产出为 `32`，周环比减少 `20.3%`；饱和度为 `101%`，单素材耗时为 `15.8` 分钟。

产出减少，主要受K2百度项目影响，其中 ` 百度网盘-APP-拉新-快手` 减少 `423` 条影响。


### 2. 效果设计部

效果设计部混剪总产出为 `466`，周环比减少 `44.0%`；人均日均产出为 `21`，周环比减少 `30.0%`；饱和度为 `79%`；单素材耗时为 `14.3` 分钟，周环比增加 `20.9%`。

其中最主要的是 `K3 · 知乎漫剧-IAA-头条` 减少 `200` 条需求

单素材耗时增加原因，上周制作简单的混剪修改的占比减少明显，上周是5%，再前一周是25%

### 3. 合肥创意部

合肥创意部混剪总产出为 `797`，人均日均产出为 `61`，周环比增加 `23.5%`；饱和度为 `118%`，单素材耗时为 `7.5` 分钟。


### 4. 品牌设计部

品牌设计部混剪总产出为 `65`，周环比减少 `46.3%`；人均日均产出为 `22`，饱和度为 `106%`，单素材耗时为 `20.5` 分钟，周环比增加 `24.3%`。

单素材耗时增长，跟效果设计部一样，主要是需求类型结构变化，混剪修改的占比减少，上周占比23%，前一周占比超过50%

产出方面，混剪周环比减少 `46.3%`，主要受 `K2 · 1688-APP-拉新-腾讯` 减少 `70` 条影响。


---

## 三、内容团队-编剧


### 1. 品牌设计部
品牌设计部上周共8个项目，人均2.7个

总产出 `49`个脚本，周环比增加 `40.0%`；人均日均产出为 `4.1`，周环比增加 `75.0%`；饱和度为 `46%`；单素材耗时为 `47.3` 分钟。

产出增加主要因 `K2 · 百度文库-H5-腾讯` 增加 `10` 个脚本。

### 2. 内容二部

内容二部共15个项目，人均2.5个

总产出为 `187`，周环比减少 `24.3%`；人均日均产出为 `7.8`，饱和度为 `95%`；单素材耗时为 `48.4` 分钟。

产出减少主要受 `品策-小红书 · 千问` 减少 `15` 个、`合肥 · 中科自理实拍的项目` 减少 `12` 个影响。


---

## 四、内容团队-摄像

摄像模块没有总计模块，因此直接按部门汇报。

### 1. 品牌设计部

品牌设计部摄像总拍摄为 `66`，周环比增加 `88.6%`；人均日均产出为 `8.3`，周环比增加 `135.7%`；饱和度为 `60%`；单素材耗时为 `30.7` 分钟，周环比下降 `23.3%`。



### 2. 内容二部

内容二部摄像总产出为 `129`，周环比减少 `31.4%`；人均日均产出为 `6.5`，周环比减少 `14.2%`；饱和度为 `61%`，单素材耗时为 `35.7` 分钟。

产出方面，主要因 `V4 · 腾讯元宝-快手` 减少 `20` 条、`合肥 · 中科自理实拍的项目` 减少 `12` 条影响。



---

## 五、内容团队-剪辑


### 1. 品牌设计部

品牌设计部剪辑总产出为 `43`，周环比增加 `16.2%`；人均日均产出为 `2.5`，周环比增加 `36.7%`；饱和度为 `37%`，单素材耗时为 `54.6` 分钟。



### 2. 内容二部

内容二部剪辑总产出为 `132`，周环比减少 `39.2%`；人均日均产出为 `4.9`，周环比减少 `24.0%`；饱和度为 `73%`，；单素材耗时为 `60.4` 分钟。


---

以上是所有的数据情况